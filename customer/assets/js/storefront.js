/* Modern Mercantile pages: native HTML UI enhanced with local-only JavaScript. */
function pageHero(title, eyebrow, description){ return `<section class="page-hero"><div class="container"><div class="breadcrumbs"><a href="index.html">Home</a> / ${title}</div><span class="eyebrow">${eyebrow}</span><h1 class="display">${title}</h1>${description?`<p>${description}</p>`:''}</div></section>`; }
function quantityControl(qty=1){return `<div class="quantity"><button type="button" data-qty="-1" aria-label="Decrease quantity">−</button><span>${qty}</span><button type="button" data-qty="1" aria-label="Increase quantity">+</button></div>`;}
function showHome(){
  const root=$('#homeContent'); if(!root)return;
  root.innerHTML=`<section class="hero"><div class="hero-slide active"><img src="${SommieStore.assets.hero}" alt="A quiet collection of objects in a sunlit gallery"></div><div class="hero-slide"><img src="https://images.unsplash.com/photo-1484101403633-562f891dc89a?auto=format&fit=crop&w=1800&q=88" alt="Curated home objects in an editorial setting"></div><div class="hero-slide"><img src="https://images.unsplash.com/photo-1601924994987-69e26d50dc26?auto=format&fit=crop&w=1800&q=88" alt="Contemporary style and everyday design objects"></div><div class="container hero-content"><div class="hero-copy"><span class="eyebrow">Edition 02 — early autumn</span><h1 class="display">Objects with<br>a point of view.</h1><p>Small runs, useful forms, and considered details selected to make the everyday feel more intentional.</p><div class="hero-actions"><a class="btn btn--signal" href="shop.html">Shop the edit ${icon('arrow')}</a><a class="btn btn--ghost" href="about.html">The Sommie approach</a></div></div></div><div class="hero-index" aria-label="Hero slides"><button class="active" aria-label="Slide 1"></button><button aria-label="Slide 2"></button><button aria-label="Slide 3"></button></div></section><section class="metric-row"><div class="metric"><strong data-counter="72">0</strong><span>Objects in the edit</span></div><div class="metric"><strong data-counter="18">0</strong><span>Independent makers</span></div><div class="metric"><strong data-counter="30">0</strong><span>Days to reconsider</span></div><div class="metric"><strong data-counter="4">0</strong><span>New drops per season</span></div></section><section class="section"><div class="container"><div class="section-head"><div><span class="eyebrow">Browse with intent</span><h2 class="section-title">Three ways in.</h2></div><a class="text-link" href="categories.html">See every category ${icon('arrow')}</a></div><div class="category-rail"><a class="category-card" href="categories.html?category=Style"><img loading="lazy" src="${SommieStore.assets.style}" alt="Selected style pieces"><div class="category-card__copy"><span>01 / Selected</span><h3>Style, sharpened.</h3><span>Explore style →</span></div></a><a class="category-card" href="categories.html?category=Home"><img loading="lazy" src="${SommieStore.assets.home}" alt="Selected home pieces"><div class="category-card__copy"><span>02 / Considered</span><h3>Home, made useful.</h3><span>Explore home →</span></div></a><a class="category-card" href="categories.html?category=Objects"><img loading="lazy" src="${SommieStore.assets.objects}" alt="Selected everyday objects"><div class="category-card__copy"><span>03 / Everyday</span><h3>Objects, in focus.</h3><span>Explore objects →</span></div></a></div></div></section><section class="section" style="padding-top:0"><div class="container"><div class="section-head"><div><span class="eyebrow">Just in</span><h2 class="section-title">New lines of thought.</h2></div><p>New pieces are added in small, deliberate groups. Start here for the latest arrivals.</p></div><div class="product-grid" id="newProducts"><div class="skeleton-card skeleton"></div><div class="skeleton-card skeleton"></div><div class="skeleton-card skeleton"></div><div class="skeleton-card skeleton"></div></div></div></section><section class="sale-band"><div class="container sale-wrap"><div><span class="eyebrow">Short run / Ends soon</span><h2 class="section-title">The quietest kind of signal.</h2><p>Selected colours and last pieces, marked down until the timer reaches zero.</p><div class="countdown" id="countdown"><div class="time-box"><b>08</b><span>Hours</span></div><div class="time-box"><b>34</b><span>Minutes</span></div><div class="time-box"><b>52</b><span>Seconds</span></div></div></div><div class="sale-product"><img loading="lazy" src="https://images.unsplash.com/photo-1542291026-7eec264c27ff?auto=format&fit=crop&w=900&q=86" alt="Studio Runner in vermilion"><div class="sale-product__copy"><span class="eyebrow">Studio runner</span><h3>Made to go everywhere.</h3><p>Our most flexible everyday pair, now in its final run of the season.</p><a class="btn btn--signal" href="product.html?id=4">See the runner</a></div></div></div></section><section class="section" style="padding-bottom:0"><div class="feature-split"><div class="feature-split__image"><img loading="lazy" src="https://images.unsplash.com/photo-1493663284031-b7e3aefcae8e?auto=format&fit=crop&w=1300&q=86" alt="A calm living space filled with practical objects"></div><div class="feature-split__copy"><span class="eyebrow">Sommie field notes</span><h2 class="section-title">Not more things. Better ones.</h2><p>We choose based on longevity, usefulness, material honesty, and a small sense of surprise.</p><a class="text-link" href="about.html">Read our approach ${icon('arrow')}</a></div></div></section><section class="newsletter"><div class="container newsletter-wrap"><div><span class="eyebrow">Stay in the loop</span><h2>Letters worth opening.</h2></div><div><p>One considered note when we have something new to show you. No constant promotions, no excess.</p><form class="newsletter-form"><input required type="email" placeholder="Your email address" aria-label="Email address"><button>Subscribe ${icon('arrow')}</button></form></div></div></section>`;
  renderProducts('#newProducts',SommieStore.products().filter(item=>item.badge==='New').slice(0,4));
  let index=0;const slides=$$('.hero-slide',root),dots=$$('.hero-index button',root);const switchSlide=next=>{index=(next+slides.length)%slides.length;slides.forEach((slide,i)=>slide.classList.toggle('active',i===index));dots.forEach((dot,i)=>dot.classList.toggle('active',i===index));};dots.forEach((dot,i)=>dot.onclick=()=>switchSlide(i));setInterval(()=>switchSlide(index+1),5500);
  const end=Date.now()+((8*60+34)*60+52)*1000;const timer=setInterval(()=>{const distance=Math.max(0,end-Date.now()),h=Math.floor(distance/3600000),m=Math.floor(distance%3600000/60000),s=Math.floor(distance%60000/1000);const boxes=$$('#countdown b',root);if(boxes.length){boxes[0].textContent=String(h).padStart(2,'0');boxes[1].textContent=String(m).padStart(2,'0');boxes[2].textContent=String(s).padStart(2,'0');}if(!distance)clearInterval(timer)},1000);
  const counters=$$('[data-counter]',root);const counterObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;const end=Number(entry.target.dataset.counter);let n=0;const id=setInterval(()=>{n=Math.min(end,n+Math.ceil(end/18));entry.target.textContent=n;if(n===end)clearInterval(id)},38);counterObserver.unobserve(entry.target)}));counters.forEach(counter=>counterObserver.observe(counter));
}
function showShop(category='All'){
  const root=$('#shopContent');if(!root)return;root.innerHTML=`${pageHero(category==='All'?'The full edit':category,category==='All'?'All categories':'Curated category','Every piece has earned its place here. Filter the collection to find the forms, materials, and colours that fit your day.') }<main class="container shop-layout"><aside class="filter-panel"><details class="filter-group" open><summary>Category</summary><div class="filter-options"><label class="filter-option"><input type="checkbox" value="Home" checked> Home</label><label class="filter-option"><input type="checkbox" value="Style" checked> Style</label><label class="filter-option"><input type="checkbox" value="Objects" checked> Objects</label></div></details><details class="filter-group"><summary>Availability</summary><div class="filter-options"><label class="filter-option"><input id="inStock" type="checkbox"> In stock</label></div></details><details class="filter-group"><summary>Price</summary><div class="filter-options"><label class="filter-option"><input type="radio" name="price" value="under100"> Under $100</label><label class="filter-option"><input type="radio" name="price" value="100to200"> $100–$200</label><label class="filter-option"><input type="radio" name="price" value="over200"> $200+</label></div></details></aside><section><div class="shop-tools"><span id="productCount">0 pieces</span><select class="select" id="sortProducts" aria-label="Sort products"><option value="featured">Sort: Featured</option><option value="low">Price: Low to high</option><option value="high">Price: High to low</option><option value="name">Name: A to Z</option></select></div><div class="collection-rule"><span>Edition 02 / Current selection</span><i></i><span>Material-led inventory</span></div><div class="product-grid product-grid--shop" id="shopProducts"></div></section></main>`;
  if(category!=='All')$$('.filter-option input',root).forEach(input=>{input.checked=input.value===category});
  const update=()=>{let items=[...SommieStore.products()];const categories=$$('.filter-group:first-child input:checked',root).map(input=>input.value);items=items.filter(item=>categories.includes(item.category));if($('#inStock',root).checked)items=items.filter(item=>item.stock>0);const price=$('input[name="price"]:checked',root)?.value;if(price==='under100')items=items.filter(item=>item.price<100);if(price==='100to200')items=items.filter(item=>item.price>=100&&item.price<=200);if(price==='over200')items=items.filter(item=>item.price>200);const sort=$('#sortProducts',root).value;if(sort==='low')items.sort((a,b)=>a.price-b.price);if(sort==='high')items.sort((a,b)=>b.price-a.price);if(sort==='name')items.sort((a,b)=>a.name.localeCompare(b.name));$('#productCount',root).textContent=`${items.length} ${items.length===1?'piece':'pieces'}`;const grid=$('#shopProducts',root);renderProducts(grid,items);grid.querySelector('.product-card')?.classList.add('product-card--feature');};$$('input,select',root).forEach(control=>control.addEventListener('change',update));update();
}
function showProduct(){const root=$('#productContent');if(!root)return;const id=Number(new URLSearchParams(location.search).get('id'))||1;const product=SommieStore.getProduct(id);if(!product){location.href='404.html';return;}SommieStore.addViewed(id);root.innerHTML=`<main class="container product-detail"><div class="breadcrumbs"><a href="index.html">Home</a> / <a href="shop.html">Shop</a> / ${product.name}</div><div class="product-detail__grid"><div class="product-gallery"><div class="thumbs"><button class="thumb active"><img src="${product.image}" alt=""></button><button class="thumb"><img src="${product.image}" alt=""></button><button class="thumb"><img src="${product.image}" alt=""></button></div><img class="primary-image" id="mainProductImage" src="${product.image}" alt="${product.name}"></div><section class="product-info"><span class="eyebrow">${product.category} ${product.badge?`/ ${product.badge}`:''}</span><h1>${product.name}</h1><p class="lead">${product.description}</p><div class="product-info__price">${product.oldPrice?`<span class="old-price">${SommieStore.formatMoney(product.oldPrice)}</span>`:''}${SommieStore.formatMoney(product.price)}</div><div class="variant-label"><span>Colour</span><span id="selectedColour">${product.colors[0]}</span></div><div class="swatches">${product.colors.map((colour,i)=>`<button type="button" class="swatch ${i===0?'selected':''}" data-colour="${colour}" style="background:${colour}" aria-label="Select ${colour}"></button>`).join('')}</div><div class="variant-label"><span>Size</span><a class="text-link" href="contact.html">Need help?</a></div><div class="size-row">${product.sizes.map((size,i)=>`<button class="size ${i===0?'selected':''}" data-size="${size}">${size}</button>`).join('')}</div><div class="buy-row">${quantityControl()}<button class="btn btn--signal" id="addProduct">Add to bag ${icon('bag')}</button><button class="icon-btn" id="detailWish" data-wishlist="${product.id}" aria-label="Save item">${icon('heart')}</button></div><div class="details-accordion"><details open><summary>Material & care</summary><p>Selected for long wear and straightforward care. Use a soft dry cloth and keep away from strong heat and direct moisture.</p></details><details><summary>Delivery & returns</summary><p>Standard delivery is free over $150. You can return unused pieces in original condition within 30 days.</p></details></div><div class="review-panel"><h3>Customer feedback</h3><p>This frontend demo does not show unverified ratings or customer reviews.</p></div></section></div></main><section class="section" style="padding-top:0"><div class="container"><div class="section-head"><div><span class="eyebrow">Keep looking</span><h2 class="section-title">Related pieces.</h2></div></div><div class="product-grid" id="relatedProducts"></div></div></section><section class="section" style="padding-top:0" id="viewedSection"><div class="container"><div class="section-head"><div><span class="eyebrow">Your path</span><h2 class="section-title">Recently viewed.</h2></div></div><div class="product-grid" id="viewedProducts"></div></div></section>`;
  let size=product.sizes[0],colour=product.colors[0],qty=1;$$(".thumb",root).forEach(thumb=>thumb.onclick=()=>{$$(".thumb",root).forEach(node=>node.classList.remove('active'));thumb.classList.add('active')});$$(".swatch",root).forEach(button=>button.onclick=()=>{$$(".swatch",root).forEach(node=>node.classList.remove('selected'));button.classList.add('selected');colour=button.dataset.colour;$('#selectedColour',root).textContent=colour});$$(".size",root).forEach(button=>button.onclick=()=>{$$(".size",root).forEach(node=>node.classList.remove('selected'));button.classList.add('selected');size=button.dataset.size});$$('[data-qty]',root).forEach(button=>button.onclick=()=>{qty=Math.max(1,qty+Number(button.dataset.qty));$('.quantity span',root).textContent=qty});$('#addProduct',root).onclick=()=>{SommieStore.addToCart(product.id,qty,{size,color:colour});updateCounts();toast(`${product.name} added to your bag.`)};$('#detailWish',root).classList.toggle('active',SommieStore.isWishlisted(product.id));renderProducts($('#relatedProducts',root),SommieStore.products().filter(item=>item.category===product.category&&item.id!==product.id).slice(0,4));const viewed=SommieStore.recentlyViewed().filter(item=>item.id!==product.id);if(viewed.length)renderProducts($('#viewedProducts',root),viewed);else $('#viewedSection',root).remove();}
function showSearch(){const root=$('#searchContent');if(!root)return;root.innerHTML=`${pageHero('Find a piece','Search the edit','Search the current collection by product name, category, material, or use.') }<main class="container section"><form class="search-form" id="productSearch"><input autofocus placeholder="Try “chair”, “studio”, or “home”" aria-label="Search products"><button aria-label="Search">${icon('search')}</button></form><p class="eyebrow" style="margin-top:24px" id="searchStatus">Start with a word, material, or category.</p><div class="product-grid" style="margin-top:28px" id="searchResults"></div></main>`;const input=$('#productSearch input',root);const search=()=>{const query=input.value.toLowerCase().trim();const matches=SommieStore.products().filter(item=>`${item.name} ${item.category} ${item.description}`.toLowerCase().includes(query));$('#searchStatus',root).textContent=query?`${matches.length} piece${matches.length===1?'':'s'} found for “${input.value}”`:'Start with a word, material, or category.';renderProducts($('#searchResults',root),query?matches:[])};$('#productSearch',root).onsubmit=event=>{event.preventDefault();search()};input.oninput=search;}
function showCart(){const root=$('#cartContent');if(!root)return;const draw=()=>{const lines=SommieStore.cartDetails();root.innerHTML=`${pageHero('Your bag','Selected pieces','Review your selected pieces before moving to the delivery details.') }<main class="container cart-layout"><section id="cartLines">${lines.length?lines.map(line=>`<article class="cart-line"><img src="${line.image}" alt="${line.name}"><div><span class="eyebrow">${line.category} / ${line.size}</span><h3>${line.name}</h3><p>${line.color?`Selected colour: ${line.color}`:''}</p><div class="cart-line__actions">${quantityControl(line.qty)}<button class="plain-button" data-remove="${line.id}" data-size="${line.size}" data-colour="${line.color}">Remove</button></div></div><div class="price">${SommieStore.formatMoney(line.price*line.qty)}</div></article>`).join(''):`<div class="empty-state"><h2>Your bag is open.</h2><p>There are no selected pieces yet. The full edit is ready when you are.</p><a class="btn btn--signal" href="shop.html">Browse the edit</a></div>`}</section><aside class="summary-card"><h2>Order summary</h2><div class="summary-row"><span>Pieces</span><span>${SommieStore.formatMoney(SommieStore.cartTotal())}</span></div><div class="summary-row"><span>Delivery</span><span>${SommieStore.cartTotal()>=150?'Complimentary':'Calculated next'}</span></div><div class="coupon"><input id="couponInput" placeholder="Coupon code" aria-label="Coupon code"><button id="couponButton">Apply</button></div><div class="summary-row total"><span>Total</span><span id="totalValue">${SommieStore.formatMoney(SommieStore.cartTotal())}</span></div><a class="btn btn--signal" style="width:100%;margin-top:21px" href="checkout.html">Continue to checkout</a></aside></main>`;$$('[data-qty]',root).forEach(button=>button.onclick=()=>{const line=button.closest('.cart-line');const id=Number($('[data-remove]',line).dataset.remove),size=$('[data-remove]',line).dataset.size,colour=$('[data-remove]',line).dataset.colour,span=$('.quantity span',line);SommieStore.updateCart(id,Math.max(1,Number(span.textContent)+Number(button.dataset.qty)),{size,color:colour});updateCounts();draw();});$$('[data-remove]',root).forEach(button=>button.onclick=()=>{SommieStore.removeCart(Number(button.dataset.remove),{size:button.dataset.size,color:button.dataset.colour});updateCounts();toast('Piece removed from your bag.');draw();});$('#couponButton',root).onclick=()=>{const code=$('#couponInput',root).value.trim().toUpperCase();if(code==='SOMMIE10'){const total=SommieStore.cartTotal()*.9;$('#totalValue',root).textContent=SommieStore.formatMoney(total);toast('SOMMIE10 applied: 10% saved.')}else toast('Try the demonstration code SOMMIE10.');};};draw();}
function showWishlist(){const root=$('#wishlistContent');if(!root)return;const items=SommieStore.wishlist().map(SommieStore.getProduct).filter(Boolean);root.innerHTML=`${pageHero('Your saved pieces','Wishlist','A private shelf for pieces you want to come back to.') }<main class="container section">${items.length?`<div class="product-grid" id="wishlistProducts"></div>`:`<div class="empty-state"><h2>Nothing saved, yet.</h2><p>Keep a running shortlist here without adding anything to your bag.</p><a class="btn btn--signal" href="shop.html">Explore the edit</a></div>`}</main>`;if(items.length)renderProducts($('#wishlistProducts',root),items);}
function showCheckout(){const root=$('#checkoutContent');if(!root)return;const lines=SommieStore.cartDetails();if(!lines.length){location.href='cart.html';return;}root.innerHTML=`${pageHero('Checkout','Delivery details','This local-only demonstration collects no payment information and does not create a real order.') }<main class="container checkout-layout"><form class="checkout-form" id="checkoutForm"><section class="form-section"><h2>Contact</h2><div class="field-grid"><div class="field full"><label>Email address</label><input required type="email" placeholder="you@example.com"></div></div></section><section class="form-section"><h2>Delivery</h2><div class="field-grid"><div class="field"><label>First name</label><input required></div><div class="field"><label>Last name</label><input required></div><div class="field full"><label>Address</label><input required></div><div class="field"><label>City</label><input required></div><div class="field"><label>Postcode</label><input required></div></div></section><section class="form-section"><h2>Delivery method</h2><div class="radio-list"><label class="radio-card"><input checked type="radio" name="shipping"> Standard delivery <span style="margin-left:auto">Free over $150</span></label><label class="radio-card"><input type="radio" name="shipping"> Priority delivery <span style="margin-left:auto">$15</span></label></div></section><section class="form-section"><h2>Payment</h2><p class="form-note">This is a non-transactional frontend demonstration. No payment is collected.</p><div class="field-grid"><div class="field full"><label>Card number</label><input disabled value="•••• •••• •••• ••••"></div></div></section><button class="btn btn--signal" type="submit">Place demonstration order</button></form><aside class="summary-card"><h2>Order summary</h2>${lines.map(line=>`<div class="summary-row"><span>${line.name} × ${line.qty}</span><span>${SommieStore.formatMoney(line.price*line.qty)}</span></div>`).join('')}<div class="summary-row total"><span>Total</span><span>${SommieStore.formatMoney(SommieStore.cartTotal())}</span></div></aside></main>`;$('#checkoutForm',root).onsubmit=event=>{event.preventDefault();localStorage.removeItem(SommieStore.storage.cart);updateCounts();root.innerHTML=`${pageHero('Order noted','Thank you','Your demonstration order has been recorded only in this browser session.') }<main class="container section"><div class="empty-state"><h2>Thank you for trying Sommie.</h2><p>This frontend-only checkout does not process payment, send confirmation emails, or create a real order. Your bag has been cleared.</p><a class="btn btn--signal" href="index.html">Return to the edit</a></div></main>`;toast('Demonstration order complete.');};}
function showAuth(){const root=$('#authContent');if(!root)return;const isRegister=document.body.dataset.page==='register';root.innerHTML=`<main class="auth-shell"><aside class="auth-image"><img src="${SommieStore.assets.style}" alt="Editorial style objects"></aside><section class="auth-card"><span class="eyebrow">${isRegister?'Sommie account':'Welcome back'}</span><h1>${isRegister?'Create your account.':'Sign in to Sommie.'}</h1><p>${isRegister?'Save pieces, keep your details close, and make every return a little faster.':'Use the details you created in this browser to continue.'}</p><form class="auth-form" id="authForm">${isRegister?`<div class="field"><label>Full name</label><input required id="authName"></div>`:''}<div class="field"><label>Email address</label><input required type="email" id="authEmail"></div><div class="field"><label>Password</label><input required type="password" minlength="4" id="authPassword"></div><button class="btn btn--signal" type="submit">${isRegister?'Create account':'Sign in'}</button></form><p class="form-note">${isRegister?'Already have a browser-local account?':'New to Sommie?'} <a class="text-link" href="${isRegister?'login.html':'register.html'}">${isRegister?'Sign in':'Create account'}</a></p></section></main>`;$('#authForm',root).onsubmit=event=>{event.preventDefault();const email=$('#authEmail').value,pass=$('#authPassword').value,user=SommieStore.getUser();if(!isRegister&&(!user||user.email!==email||user.pass!==pass)){toast('Those browser-local details do not match. Create an account first.');return;}SommieStore.setUser({name:isRegister?$('#authName').value:user.name,email,pass});toast(isRegister?'Account created locally in this browser.':'Signed in locally.');setTimeout(()=>location.href='account.html',650);};}
function showAccount(){const root=$('#accountContent');if(!root)return;const user=SommieStore.getUser();if(!user){location.href='login.html';return;}root.innerHTML=`${pageHero('Your account','Browser-local profile',`Signed in as ${user.name||user.email}.`) }<main class="container section"><div class="empty-state"><h2>Hello, ${user.name||'there'}.</h2><p>Your profile, cart, wishlist, and theme are stored only in this browser through local storage. No information is sent to a server.</p><button class="btn btn--ghost" id="signOut">Sign out</button></div></main>`;$('#signOut').onclick=()=>{localStorage.removeItem(SommieStore.storage.user);toast('Signed out from this browser.');setTimeout(()=>location.href='index.html',450);};}
function showContact(){const form=$('#contactForm');if(form)form.onsubmit=event=>{event.preventDefault();form.reset();toast('Message saved for this frontend demonstration.');};}
document.addEventListener('DOMContentLoaded',()=>{const page=document.body.dataset.page;({home:showHome,shop:()=>showShop('All'),categories:()=>showShop(new URLSearchParams(location.search).get('category')||'All'),product:showProduct,search:showSearch,cart:showCart,wishlist:showWishlist,checkout:showCheckout,login:showAuth,register:showAuth,account:showAccount}[page]||(()=>{}))();showContact();});
