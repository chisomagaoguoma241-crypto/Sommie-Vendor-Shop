/* Modern Mercantile interaction layer: direct actions, tactile feedback, browser-local state. */
const $ = (selector, parent=document) => parent.querySelector(selector);
const $$ = (selector, parent=document) => [...parent.querySelectorAll(selector)];

function icon(name) {
  const paths={bag:'<path d="M6 8h12l1 13H5L6 8Z"/><path d="M9 8a3 3 0 0 1 6 0"/>',heart:'<path d="M20.8 4.6a5.5 5.5 0 0 0-7.8 0L12 5.7l-1.1-1.1a5.5 5.5 0 0 0-7.8 7.8L12 21l8.9-8.6a5.5 5.5 0 0 0-.1-7.8Z"/>',search:'<circle cx="11" cy="11" r="6.8"/><path d="m16.2 16.2 4.2 4.2"/>',menu:'<path d="M4 7h16M4 12h16M4 17h16"/>',x:'<path d="m6 6 12 12M18 6 6 18"/>',sun:'<circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41"/>',moon:'<path d="M21 12.8A9 9 0 1 1 11.2 3 7 7 0 0 0 21 12.8Z"/>',arrow:'<path d="M5 12h14M13 6l6 6-6 6"/>',chevronUp:'<path d="m6 15 6-6 6 6"/>',user:'<circle cx="12" cy="8" r="3.2"/><path d="M5.5 21a6.5 6.5 0 0 1 13 0"/>'};
  return `<svg aria-hidden="true" class="icon" viewBox="0 0 24 24">${paths[name]||''}</svg>`;
}

function header() {
  const path=location.pathname.split('/').pop()||'index.html';
  const active=target=>path===target?'active':'';
  return `<div class="topbar"><div class="ticker"><span>Complimentary shipping over $150</span><span>Curated objects, selected weekly</span><span>Returns within 30 days</span><span>Complimentary shipping over $150</span><span>Curated objects, selected weekly</span></div></div>
  <header class="site-header" id="siteHeader"><div class="container header-row"><nav class="primary-nav" aria-label="Primary"><a class="${active('shop.html')}" href="shop.html">Shop</a><a class="${active('categories.html')}" href="categories.html">Categories</a><a class="${active('about.html')}" href="about.html">Our story</a></nav><a class="brand" href="index.html" aria-label="Sommie Vendor Shop home"><img src="${SommieStore.assets.logo}" alt=""><span class="brand-word">S<span class="brand-o">o</span>mmie</span><small>Vendor Shop</small></a><div class="header-actions"><a class="icon-btn header-search" href="search.html" aria-label="Search">${icon('search')}</a><a class="icon-btn" href="wishlist.html" aria-label="Wishlist">${icon('heart')}<b class="count" id="wishlistCount">0</b></a><a class="icon-btn" href="cart.html" aria-label="Bag">${icon('bag')}<b class="count" id="cartCount">0</b></a><button class="icon-btn" id="themeToggle" aria-label="Toggle color mode">${icon('moon')}</button><button class="icon-btn mobile-toggle" id="mobileToggle" aria-label="Open navigation">${icon('menu')}</button></div></div></header>
  <nav class="mobile-nav" id="mobileNav" aria-label="Mobile navigation"><a href="shop.html">Shop all pieces</a><a href="categories.html">Categories</a><a href="about.html">Our story</a><a href="wishlist.html">Wishlist</a><a href="account.html">Account</a><a href="contact.html">Contact</a></nav>`;
}

function footer() { return `<footer class="site-footer"><div class="container"><div class="footer-grid"><div class="footer-brand"><a class="brand" href="index.html"><img src="${SommieStore.assets.logo}" alt=""><span class="brand-word">S<span class="brand-o">o</span>mmie</span></a><p>A slower, sharper way to collect the things you live with.</p></div><div><h4>Shop</h4><a href="shop.html">All pieces</a><a href="categories.html?category=Style">Style</a><a href="categories.html?category=Home">Home</a><a href="categories.html?category=Objects">Objects</a></div><div><h4>Help</h4><a href="contact.html">Contact</a><a href="faq.html">FAQ</a><a href="shipping.html">Delivery & returns</a><a href="privacy.html">Privacy</a></div><div><h4>Elsewhere</h4><a href="about.html">About Sommie</a><a href="terms.html">Terms</a><a href="account.html">Account</a><a href="../admin/index.html">Vendor dashboard</a></div></div><div class="footer-base"><span>© ${new Date().getFullYear()} Sommie Vendor Shop</span><span>Designed for considered living</span></div></div></footer>`; }

function modal() { return `<div class="modal-backdrop" id="newsletterModal" role="dialog" aria-modal="true" aria-labelledby="modalTitle"><div class="modal"><button class="icon-btn modal-close" data-close-modal aria-label="Close">${icon('x')}</button><span class="eyebrow">Field notes</span><h2 id="modalTitle">A little more considered.</h2><p>Receive new objects, selected stories, and early access to limited runs. No noise.</p><form class="modal-form" id="modalSubscribe"><input required type="email" placeholder="Your email address" aria-label="Email address"><button>Subscribe</button></form></div></div><div class="toast-stack" id="toastStack" aria-live="polite"></div><button class="scroll-top" id="scrollTop" aria-label="Back to top">${icon('chevronUp')}</button><div class="loading" id="loading"><div class="loading-mark" aria-label="Loading"></div></div>`; }

function updateCounts(){ const cartQty=SommieStore.cart().reduce((sum,line)=>sum+line.qty,0); const wish=SommieStore.wishlist().length; const cart=$('#cartCount'),wishlist=$('#wishlistCount'); if(cart)cart.textContent=cartQty; if(wishlist)wishlist.textContent=wish; }
function toast(message){ const stack=$('#toastStack'); if(!stack)return; const node=document.createElement('div'); node.className='toast'; node.innerHTML=`<span>${message}</span><button aria-label="Dismiss">×</button>`; $('button',node).onclick=()=>node.remove(); stack.append(node); setTimeout(()=>node.remove(),4500); }
function productCard(product){ const inWish=SommieStore.isWishlisted(product.id); return `<article class="product-card reveal"><a class="product-card__image" href="product.html?id=${product.id}"><img loading="lazy" src="${product.image}" alt="${product.name}">${product.badge?`<span class="product-card__tag">${product.badge}</span>`:''}</a><button class="icon-btn product-card__wish ${inWish?'active':''}" data-wishlist="${product.id}" aria-label="Save ${product.name}">${icon('heart')}</button><div class="product-card__quick"><button class="btn" data-quick-add="${product.id}">Add to bag</button></div><div class="product-card__meta"><div><h3><a href="product.html?id=${product.id}">${product.name}</a></h3><p>${product.category}</p></div><div class="price">${product.oldPrice?`<span class="old-price">${SommieStore.formatMoney(product.oldPrice)}</span>`:''}${SommieStore.formatMoney(product.price)}</div></div></article>`; }
function renderProducts(target, products){ const root=typeof target==='string'?$(target):target; if(root)root.innerHTML=products.map(productCard).join(''); observeReveals(); }
function addQuick(id){ const product=SommieStore.getProduct(id); SommieStore.addToCart(id,1,{size:product.sizes[0],color:product.colors[0]}); updateCounts(); toast(`${product.name} added to your bag.`); }
function observeReveals(){ const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.1}); $$('.reveal').forEach(node=>observer.observe(node)); }

function bindGlobalEvents(){
  document.addEventListener('click', event=>{ const wish=event.target.closest('[data-wishlist]'); if(wish){const id=Number(wish.dataset.wishlist);const saved=SommieStore.toggleWishlist(id);wish.classList.toggle('active',saved);updateCounts();toast(saved?'Saved to your wishlist.':'Removed from your wishlist.');} const quick=event.target.closest('[data-quick-add]');if(quick)addQuick(Number(quick.dataset.quickAdd)); if(event.target.closest('[data-close-modal]')) $('#newsletterModal')?.classList.remove('open'); });
  $('#themeToggle')?.addEventListener('click',()=>{const next=SommieStore.getTheme()==='dark'?'light':'dark';SommieStore.setTheme(next);$('#themeToggle').innerHTML=icon(next==='dark'?'sun':'moon');});
  $('#mobileToggle')?.addEventListener('click',()=>{const nav=$('#mobileNav');nav.classList.toggle('open');$('#mobileToggle').innerHTML=icon(nav.classList.contains('open')?'x':'menu');});
  $('#scrollTop')?.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));
  window.addEventListener('scroll',()=>{$('#siteHeader')?.classList.toggle('is-scrolled',scrollY>8);$('#scrollTop')?.classList.toggle('show',scrollY>500)});
  $$('.newsletter-form').forEach(form=>form.addEventListener('submit',event=>{event.preventDefault();toast('You are on the list. Expect considered notes only.');event.target.reset();}));
  $('#modalSubscribe')?.addEventListener('submit',event=>{event.preventDefault();$('#newsletterModal').classList.remove('open');toast('Subscribed. The first note is on its way.');});
  $('#newsletterModal')?.addEventListener('click',event=>{if(event.target.id==='newsletterModal')event.target.classList.remove('open')});
}

document.addEventListener('DOMContentLoaded',()=>{document.body.insertAdjacentHTML('afterbegin',header());document.body.insertAdjacentHTML('beforeend',footer()+modal());SommieStore.setTheme(SommieStore.getTheme());$('#themeToggle').innerHTML=icon(SommieStore.getTheme()==='dark'?'sun':'moon');updateCounts();bindGlobalEvents();observeReveals();setTimeout(()=>$('#loading')?.classList.add('done'),350);setTimeout(()=>$('#newsletterModal')?.classList.add('open'),4000);});
